<%# Only show flash messages if they exist %>
<% if (typeof messages === 'function') { %>
  <% const flashMessages = messages(); %>
  <% if (flashMessages) { %>
    <div class="flash-message"><%- flashMessages %></div>
  <% } %>
<% } %>